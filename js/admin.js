let announcements = []; let schedules = [];
function actionButton(text, className, handler) { const b = make('button', className, text); b.type = 'button'; b.addEventListener('click', handler); return b; }
function adminNotice(item) { const row = make('article', 'admin-row'); const info = make('div'); info.append(make('h3', '', item.title), make('p', 'muted', `${formatDateTime(item.created_at)}${item.is_important ? ' · 중요 공지' : ''}`)); const actions = make('div', 'row-actions'); actions.append(actionButton('수정', 'small-button', () => editNotice(item)), actionButton(item.is_important ? '고정 해제' : '중요 고정', 'small-button', () => toggleImportant(item)), actionButton('삭제', 'small-button danger', () => deleteNotice(item))); row.append(info, actions); return row; }
function adminSchedule(item) { const row = make('article', 'admin-row'); const info = make('div'); info.append(make('h3', '', item.title), make('p', 'muted', formatDate(item.event_date))); const actions = make('div', 'row-actions'); actions.append(actionButton('수정', 'small-button', () => editSchedule(item)), actionButton('삭제', 'small-button danger', () => deleteSchedule(item))); row.append(info, actions); return row; }
async function loadAdminData() { const [a, s] = await Promise.all([supabaseClient.from('announcements').select('*').order('created_at', { ascending: false }), supabaseClient.from('schedules').select('*').order('event_date')]); if (a.error || s.error) { $('#admin-error').textContent = '관리 목록을 불러오지 못했습니다. 관리자 권한을 확인해주세요.'; return; } announcements = a.data; schedules = s.data; const at = $('#admin-announcements'); const st = $('#admin-schedules'); at.replaceChildren(...announcements.map(adminNotice)); st.replaceChildren(...schedules.map(adminSchedule)); if (!announcements.length) renderEmpty(at, '등록된 공지가 없습니다.'); if (!schedules.length) renderEmpty(st, '등록된 일정이 없습니다.'); }
function showForm(id) { $(id).hidden = false; $(id).scrollIntoView({ behavior: 'smooth', block: 'center' }); }
function hideForm(id) { const form = $(id); form.reset(); $('[name="id"]', form).value = ''; form.hidden = true; }
function editNotice(item) { const f = $('#announcement-form'); $('[name="id"]', f).value = item.id; $('[name="title"]', f).value = item.title; $('[name="content"]', f).value = item.content; $('[name="is_important"]', f).checked = item.is_important; showForm('#announcement-form'); }
function editSchedule(item) { const f = $('#schedule-form'); $('[name="id"]', f).value = item.id; $('[name="event_date"]', f).value = item.event_date; $('[name="title"]', f).value = item.title; $('[name="description"]', f).value = item.description || ''; showForm('#schedule-form'); }
async function toggleImportant(item) { const { error } = await supabaseClient.from('announcements').update({ is_important: !item.is_important }).eq('id', item.id); if (error) return showToast('중요 공지 설정을 변경하지 못했습니다.', true); showToast('중요 공지 설정을 변경했습니다.'); loadAdminData(); }
async function deleteNotice(item) { if (!confirm(`정말 '${item.title}' 공지를 삭제하시겠습니까?`)) return; const { error } = await supabaseClient.from('announcements').delete().eq('id', item.id); if (error) return showToast('공지를 삭제하지 못했습니다.', true); showToast('공지를 삭제했습니다.'); loadAdminData(); }
async function deleteSchedule(item) { if (!confirm(`정말 '${item.title}' 일정을 삭제하시겠습니까?`)) return; const { error } = await supabaseClient.from('schedules').delete().eq('id', item.id); if (error) return showToast('일정을 삭제하지 못했습니다.', true); showToast('일정을 삭제했습니다.'); loadAdminData(); }
async function saveNotice(e) { e.preventDefault(); const f = e.currentTarget, d = new FormData(f), id = d.get('id'), payload = { title: d.get('title').trim(), content: d.get('content').trim(), is_important: d.get('is_important') === 'on' }; const q = id ? supabaseClient.from('announcements').update(payload).eq('id', id) : supabaseClient.from('announcements').insert(payload); const { error } = await q; if (error) return showToast('공지를 저장하지 못했습니다. 권한을 확인해주세요.', true); hideForm('#announcement-form'); showToast('공지를 저장했습니다.'); loadAdminData(); }
async function saveSchedule(e) { e.preventDefault(); const f = e.currentTarget, d = new FormData(f), id = d.get('id'), payload = { title: d.get('title').trim(), description: d.get('description').trim(), event_date: d.get('event_date') }; const q = id ? supabaseClient.from('schedules').update(payload).eq('id', id) : supabaseClient.from('schedules').insert(payload); const { error } = await q; if (error) return showToast('일정을 저장하지 못했습니다. 권한을 확인해주세요.', true); hideForm('#schedule-form'); showToast('일정을 저장했습니다.'); loadAdminData(); }
document.addEventListener('DOMContentLoaded', async () => { if (!supabaseClient) { location.replace('login.html'); return; } const { data: { session } } = await supabaseClient.auth.getSession(); if (!session) { location.replace('login.html'); return; }
  // 화면 접근도 확인하지만, 실제 변경 권한은 Supabase RLS가 다시 검사합니다.
  const { data: profile, error: profileError } = await supabaseClient.from('admin_profiles').select('is_admin').eq('id', session.user.id).maybeSingle();
  if (profileError || !profile?.is_admin) { await supabaseClient.auth.signOut(); location.replace('login.html'); return; }
  $('#logout-button').addEventListener('click', async () => { await supabaseClient.auth.signOut(); location.replace('login.html'); }); $('#new-announcement').addEventListener('click', () => showForm('#announcement-form')); $('#new-schedule').addEventListener('click', () => showForm('#schedule-form')); $('#announcement-form').addEventListener('submit', saveNotice); $('#schedule-form').addEventListener('submit', saveSchedule); document.querySelectorAll('.cancel-edit').forEach(b => b.addEventListener('click', () => hideForm(`#${b.closest('form').id}`))); loadAdminData(); });
